## Context Transfer from Claude Session

**IMPORTANT**: This is a context handoff from a Claude session.
The previous AI assistant completed the work described below.
Please review and continue from where it left off.

**Source Provider**: Claude
**Source Session**: e186ed30-8928-4b6d-b99c-1463928087ea
**Transferred**: 2026-04-05 23:54:53
**Conversations**: 7

---

### Session Activity Summary

**Tool Calls:**
- Read: 36
- Grep: 19
- Edit: 8
- Agent: 4
- AskUserQuestion: 3
- Glob: 2
- TaskUpdate: 2
- Write: 1
- ExitPlanMode: 1
- TaskCreate: 1

**Files Created/Written:**
- `C:\Users\imzmq\.claude\plans\bubbly-snuggling-treehouse.md`
- `sql\data.sql`

**Files Read:**
- `D:\Practice\InterviewReview\sql\schema.sql`
- `D:\Practice\InterviewReview\sql\data.sql`
- `D:\Practice\InterviewReview\src\main\java\com\example\interview\agent\task\TaskType.java`
- `D:\Practice\InterviewReview\src\main\java\com\example\interview\agent\TaskRouterAgent.java`
- `D:\Practice\InterviewReview\src\main\java\com\example\interview\service\IntentTreeRoutingService.java`
- `D:\Practice\InterviewReview\src\main\java\com\example\interview\service\PromptManager.java`
- `D:\Practice\InterviewReview\src\main\java\com\example\interview\service\WebChatService.java`
- `D:\Practice\InterviewReview\src\main\java\com\example\interview\intent\IntentTreeNode.java`
- `D:\Practice\InterviewReview\src\main\java\com\example\interview\service\impl\IntentTreeServiceImpl.java`
- `D:\Practice\InterviewReview\src\main\java\com\example\interview\config\IntentTreeProperties.java`
- ... and 8 more

**Tasks:** 1/1 completed

**Recent Tool Executions:**

- **Edit**
  - Input: D:\Practice\InterviewReview\sql\data.sql
  - Result: `The file D:\Practice\InterviewReview\sql\data.sql has been updated successfully.`
- ... and 72 more

---

### Previous Conversation Context

#### Turn 1
**User**: 我现在在AI助手页面，我想面试的话会被识别成知识问答，我想你根据我项目的功能帮我配置好我的意图树，就放在那个data.sql文件里，让它不会识别错我的各种功能

**Assistant**: Let me explore the codebase to understand the intent tree structure and routing logic.

---

#### Turn 2
**User**: 你从一个重度使用的用户的角度来优化一下这个意图树，因为用户的问题的输入总是千奇百怪的；

**Assistant**: 让我重新审视当前的意图树，从一个真实重度用户的角度思考各种刁钻输入。

---

#### Turn 3
**User**: 我现在给AI助手发送：给我来一场Java面试，跳过自我介绍，然后它回我：根据检索到的知识库内容，我无法直接为您模拟一场完整的Java面试。知识库中只包含了一些零散的Java技术知识点，例如Java内存模型（JMM）和HandlerInterceptor与Filter的区别，但缺乏构成一场结构化面试所需的完整问题列表、评估标准和互动流程。

不过，我可以基于知识库中已有的内容，为您设计几个相关的面试问题作为示例：

1. 请解释一下什么是Java内存模型（JMM），以及它要解决所属框架：HandlerInterceptor 是Spring MVC框架内的组件，而Filter是Java Web标准（Servlet规范）定义的。2）执行时机：HandlerInterceptor 在DispatcherServlet之后、Controller之前执行；Filter则在请求进入Tomcat后、到达Dispatc……

**Assistant**: 这说明你的输入要么没走到意图树分类，要么意图树分类后仍然被识别成了 KNOWLEDGE_QA。让我排查根因。

---

#### Turn 4
**User**: This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
1. Primary Request and Intent:
   The user has an AI interview review assistant system. When using the AI assistant chat page (`/chat`), typing interview-related inputs (e.g., "给我来一场Java面试，跳过自我介绍") gets misidentified as `KNOWLEDGE_QA` (knowledge Q&A) instead of `INTERVIEW_START` (mock interview). The user wants the intent tree (`t_intent_node` table) configured properly in `sql/data.sql` so that all system features are correctly identified. The user also requested optimization from a heavy user's perspective to handle diverse, colloquial, and unexpected user inputs. After both rounds of optimization, the user reports the issue persists — the system still routes interview requests to the KnowledgeQaAgent (RAG-based knowledge Q&A).

2. Key Technical Concepts:
   - **Intent Tree (意图树)**: Three-level hierarchy stored in `t_intent_node` — Level 0 (DOMAIN), Level 1 (CATEGORY), Level 2 (LEAF/executable intents). Each leaf has `intent_code`, `description`, `examples` (JSON array for few-shot), `slot_hints`, and `task_type` binding.
   - **Intent Classification Flow**: `WebChatService.dispatchChat()` → `TaskRouterAgent.dispatch()` (taskType=null) → `treeIntentDispatch()` → `IntentTreeRoutingService.route()` → LLM classifier using `intent-tree-classifier` prompt template → `normalizeDecision()` → re-dispatch with resolved TaskType.
   - **Fallback chain**: If intent tree disabled or fails → `reactDispatch()` using `task-router` prompt template → string-matching on LLM output.
   - **Clarification mechanism**: `shouldClarify()` checks confidence < threshold (0.60), top1-top2 gap < minGap (0.12), ambiguity ratio >= 0.80, or missing critical slots for CODING_PRACTICE.
   - **Slot Refinement**: Two-phase — coarse intent classification first, then `refineSlots()` with dynamic few-shot examples from `t_intent_slot_refine_case`.
   - **`loadAllLeafIntents()`**: Queries ALL nodes where `enabled=true` (no level filter). Parent nodes MUST have `enabled=0` to avoid polluting classification.
   - **Prompt templates**: Stored in `t_prompt_template` database table, loaded by `PromptManager`. The `intent-tree-classifier` template is what the LLM actually sees for classification. We have NOT examined its content.
   - **Cache**: Intent tree is cached with Spring `@Cacheable(value = "intentTree", key = "'allLeafIntents'")`. Cache must be evicted after data changes.

3. Files and Code Sections:
   - **`sql/data.sql`** (MODIFIED — the primary file changed)
     - This is the only file modified. Contains INSERT statements for `t_intent_node` and `t_intent_slot_refine_case`.
     - **Current state after two rounds of optimization**:
       - L0 DOMAIN nodes (4): INTERVIEW, CODING, KNOWLEDGE, PROFILE — all `enabled=0`
       - L1 CATEGORY nodes (4): INTERVIEW.LIFECYCLE, CODING.PRACTICE, KNOWLEDGE.QA, PROFILE.TRAINING — all `enabled=0`
       - L2 LEAF nodes (12): INTERVIEW.START.GENERAL (38 examples), INTERVIEW.ANSWER.GENERAL, INTERVIEW.REPORT.GENERAL, **CODING.PRACTICE.GENERAL (new catch-all)**, CODING.PRACTICE.CHOICE/FILL/ALGORITHM/SCENARIO, KNOWLEDGE.QA.GENERAL (16 examples, with negative guidance), PROFILE.TRAINING.QUERY, UNKNOWN
       - Slot refine cases: 27 total, organized by taskType with segmented sort_order
     - Key description for INTERVIEW.START.GENERAL: `'用户想要【开始】一场模拟面试...面试的本质是"你来考我/问我"，知识问答的本质是"我来问你"'`
     - Key description for KNOWLEDGE.QA.GENERAL: `'...【重要区分】如果用户说"面试""面我""考我""模拟"，那是面试（INTERVIEW_START）...'`

   - **`sql/schema.sql`** (READ ONLY)
     - `t_intent_node` table: id, intent_code (UNIQUE), name, description, parent_code, level, examples (JSON), slot_hints (JSON), task_type, enabled, deleted
     - `t_intent_slot_refine_case` table: id, task_type, user_query, ai_output, sort_order, enabled, deleted. UNIQUE KEY on (task_type, user_query, deleted)

   - **`src/main/java/com/example/interview/agent/task/TaskType.java`** (READ ONLY)
     - Enum: INTERVIEW_START, INTERVIEW_ANSWER, INTERVIEW_REPORT, LEARNING_PLAN, CODING_PRACTICE, PROFILE_EVENT_UPSERT, PROFILE_SNAPSHOT_QUERY, PROFILE_TRAINING_PLAN_QUERY, KNOWLEDGE_QA

   - **`src/main/java/com/example/interview/agent/TaskRouterAgent.java`** (READ ONLY)
     - `dispatch()`: If taskType==null → `treeIntentDispatch()`. Otherwise switch on TaskType to route to specific agent.
     - `treeIntentDispatch()`: Calls `intentTreeRoutingService.route()`, handles clarification, resolves TaskType via `TaskType.valueOf()`, builds new payload with slots, re-dispatches.
     - `reactDispatch()`: Legacy fallback using `task-router` prompt template with string-matching on LLM output.

   - **`src/main/java/com/example/interview/service/IntentTreeRoutingService.java`** (READ ONLY)
     - `route()`: Loads leaf intents, renders `intent-tree-classifier` prompt via `promptManager.renderSplit("router", "intent-tree-classifier", vars)`, calls LLM, normalizes decision.
     - `shouldClarify()`: Checks confidence threshold, candidate gap, ambiguity ratio, missing slots.
     - `loadLeafIntents()`: Calls `intentTreeService.loadAllLeafIntents()`, falls back to hardcoded `defaultLeafIntents()` if empty.
     - `refineSlots()`: Uses `intent-slot-refine` prompt template with dynamic few-shot cases.

   - **`src/main/java/com/example/interview/service/impl/IntentTreeServiceImpl.java`** (READ ONLY)
     - `loadAllLeafIntents()`: Queries ALL nodes where `enabled=true` (NO level filter!). Then calls `ensureCodingLeafIntents()` to auto-insert missing coding sub-types.
     - Cached with `@Cacheable(value = "intentTree", key = "'allLeafIntents'")`.

   - **`src/main/java/com/example/interview/service/WebChatService.java`** (READ ONLY)
     - `dispatchChat()`: Creates `TaskRequest(null, payload, context)` — null taskType triggers intent classification.

   - **`src/main/java/com/example/interview/service/PromptManager.java`** (READ ONLY)
     - Templates loaded from `t_prompt_template` database table. Required templates include "intent-tree-classifier", "intent-clarification", "intent-slot-refine", "task-router", etc.
     - `renderSplit()` returns PromptPair(systemPrompt, userPrompt).

   - **`src/main/java/com/example/interview/config/IntentTreeProperties.java`** (READ ONLY)
     - Config prefix `app.intent-tree`. Defaults: enabled=true, confidenceThreshold=0.6, minGap=0.12, ambiguityRatio=0.8, clarificationTtlMinutes=10, maxCandidates=3, fallbackToLegacyTaskRouter=true.

   - **`src/main/resources/application.yml`** (READ ONLY)
     - `app.intent-tree.enabled: true`, confidence-threshold: 0.60, min-gap: 0.12, ambiguity-ratio: 0.80

4. Errors and Fixes:
   - **Parent nodes polluting classification**: Initially set L0/L1 nodes with `enabled=1`. Since `loadAllLeafIntents()` queries ALL enabled nodes regardless of level, these parent nodes (with no taskType) would be sent to the LLM classifier as candidates. Fixed by setting all L0/L1 nodes to `enabled=0`.
   - **Explore agents failed**: API returned 403 for claude-haiku-4-5-20251001 model. Worked around by doing direct Glob/Grep/Read searches instead.
   - **Intent still misclassified after data.sql changes**: User reported "给我来一场Java面试，跳过自我介绍" still routes to KNOWLEDGE_QA. Investigation was started but interrupted. Likely causes: (a) data.sql not re-executed against database, (b) intent tree cache not cleared, (c) the `intent-tree-classifier` prompt template in `t_prompt_template` table may be poorly structured — we have NOT examined its content.

5. Problem Solving:
   - Root cause of original misclassification identified: too few examples in INTERVIEW.START, too broad KNOWLEDGE.QA description, missing parent nodes in tree structure.
   - Two rounds of data.sql optimization completed with comprehensive examples and discriminative descriptions.
   - New CODING.PRACTICE.GENERAL catch-all node added for unspecified "刷题" inputs.
   - **Still unresolved**: The user's latest test shows the fix isn't working. The actual prompt template `intent-tree-classifier` stored in `t_prompt_template` database table has never been examined — this is the prompt the LLM actually sees when classifying intents. Also, the data.sql changes may not have been applied to the running database, and the intent tree cache may not have been cleared.

6. All User Messages:
   - "我现在在AI助手页面，我想面试的话会被识别成知识问答，我想你根据我项目的功能帮我配置好我的意图树，就放在那个data.sql文件里，让它不会识别错我的各种功能" (Initial request: fix intent tree misclassification)
   - Selected "也新增面试回答节点" (Yes, add INTERVIEW.ANSWER.GENERAL node)
   - "你从一个重度使用的用户的角度来优化一下这个意图树，因为用户的问题的输入总是千奇百怪的" (Optimize from heavy user perspective for diverse inputs)
   - "我现在给AI助手发送：给我来一场Java面试，跳过自我介绍，然后它回我：根据检索到的知识库内容，我无法直接为您模拟一场完整的Java面试..." (Bug report: still misidentified as KNOWLEDGE_QA after changes)

7. Pending Tasks:
   - **Investigate why the intent tree fix isn't working**: The data.sql changes may need to be applied to the database; the intent tree cache may need clearing; the `intent-tree-classifier` prompt template content (stored in `t_prompt_template` DB table) needs examination — it's the actual prompt the LLM uses for classification and has never been reviewed.
   - Verify the fix works end-to-end with the user's test case: "给我来一场Java面试，跳过自我介绍" → should route to INTERVIEW_START.

8. Current Work:
   The user reported that after both rounds of data.sql optimization, sending "给我来一场Java面试，跳过自我介绍" on the AI assistant page STILL gets classified as KNOWLEDGE_QA. The response shows RAG retrieval behavior ("根据检索到的知识库内容..."), confirming it went through `KnowledgeQaAgent` instead of `InterviewOrchestratorAgent`.

   I had just started investigating by checking:
   - `application.yml`: intent-tree enabled=true, confidence-threshold=0.60 (confirmed)
   - Searched for prompt template files for `intent-tree-classifier` and `task-router` — found they're only in `sql/schema.sql` (table definition), confirming they live in the `t_prompt_template` database table, not in files.

   The investigation was interrupted before I could:
   1. Check if the data.sql was actually re-executed against the live database
   2. Examine the `intent-tree-classifier` prompt template content in the DB
   3. Check if the intent tree cache was cleared after changes

9. Optional Next Step:
   Continue investigating WHY the intent classification still fails despite data.sql changes. The user's exact words: "我现在给AI助手发送：给我来一场Java面试，跳过自我介绍，然后它回我：根据检索到的知识库内容..."

   Key investigation paths:
   1. Ask the user if they re-ran the data.sql against the database and cleared the intent tree cache (restart or API call)
   2. Examine the `intent-tree-classifier` prompt template — this is the LLM prompt that actually performs classification and we've never seen its content. It's stored in `t_prompt_template` table. Look for it in migration scripts, seed data, or ask user to provide it.
   3. Check if there are any other prompt template SQL files or initialization code that populates `t_prompt_template` — search for INSERT INTO t_prompt_template

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: C:\Users\imzmq\.claude\projects\D--Practice-InterviewReview\e186ed30-8928-4b6d-b99c-1463928087ea.jsonl
Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.

**Assistant**: [Tools: Grep 1 file(s): intent-tree-classifier]

---

#### Turn 5
**User**: intent-tree-classifier
你是一个“树形意图分类器”。你的任务是：从用户输入中识别最匹配的【叶子意图】并输出可路由的结构化结果。

你会收到：
1) 用户当前输入：{{ query }}
2) 对话上下文。
5) 主题/槽位抽取必须保守：不确定就留空字符串或 null，不允许凭空补全。
6) 如果用户表达明显是闲聊/无意图，返回 UNKNOWN（taskType=UNKNOWN，intentId="UNKNOWN"）。

澄清触发规则（任一满足即触发）：
- top1Score < {{ confidenceThreshold | default(0.6) }}
- top1Score - top2Score < {{ minGap | default(0.12) }}
- top2Score / max(top1Score, 0.0001) >= {{ ambiguityRatio | default(0.8) }}
- 缺少该意图的关键槽位（例如 CODING_PRACTICE 缺少题型/主题，INTERVIEW_START 缺少面试主题等）

输出 JSON 结构（字段必须齐全）：
{
  "intentId": "...",
  "taskType": "...",
  "confidence": 0.0,
  "reason": "...",
  "slots": {
    "topic": "",
    "questionType": "",
    "difficulty": "",
    "count": null,
    "skipIntro": null,
    "mode": ""
  },
  "candidates": [
    { "intentId": "...", "taskType": "...", "score": 0.0, "reason": "...", "missingSlots": ["..."] }
  ],
  "askClarification": false,
  "clarificationQuestion": "",
  "clarificationOptions": [
    { "label": "", "intentId": "", "hint": "" }
  ]
}

槽位抽取规范：
- topic：优先抽取技术栈/主题词（如 Java 并发、Spring Boot、Redis、JVM、MySQL、算法等）。
- questionType：仅限 "ALGORITHM" / "CHOICE" / "FILL"；若用户说“选择题/单选/多选”，用 CHOICE；若说“填空/补全”，用 FILL；若用户未明确题型则保持空字符串，并在 missingSlots 中标记 questionType。
- difficulty：仅限 "easy" / "medium" / "hard"；不确定留空。
- count：题目数量，无法确定用 null。
- skipIntro：用户明确要求“跳过自我介绍/直接出题/跳过介绍”时为 true，否则为 null。
- mode：学习画像/计划查询时可用（如 "interview" / "learning"），不确定留空。

请先在内部完成打分与排序，然后严格按上述 JSON 输出最终结果。

router
你是一个智能任务路由网关。你的职责是准确识别用户意图并分发到对应的处理模块。
必须输出严格 JSON，不允许多余文字。

**Assistant**: **找到根因了！** `intent-tree-classifier` 模板中 **完全没有渲染 `leafIntents` 变量**。LLM 根本看不到你配置的意图列表（包括 descriptions 和 examples），所以只能瞎猜，自然就把面试请求误判为知识问答了。

代码中 `IntentTreeRoutingService.route()` 传了 `vars.put("leafIntents", leafIntents)`，但模板里从未使用 `{{ leafIntents }}`。

需要两步修复：

**第1步：立即在 MySQL 中执行以下 UPDATE**（修复 prompt 模板）：

---

#### Turn 6
**User**: 那正好你帮我把全部提示词都查看一下是否有错，然后再优化一下

**Assistant**: 好，我先梳理所有模板的调用点和变量绑定，然后请你导出数据库内容。

---

#### Turn 7
**User**: coding-coach,SYSTEM,"你是一位专业的技术教练。你的职责是帮助候选人提升技术能力。

根据候选人的水平调整难度，低分时回归基础，高分时深入进阶。"

context-compressor,SYSTEM,你是一个对话上下文压缩器。你的职责是将对话历史压缩为结构化摘要，只保留关键信息。

interviewer,SYSTEM,"你是一位资深技术面试官。你的职责是公正、专业地评估候选人的技术能力。

评分维度包括：知识点准确性、表达逻辑、深度理解、边界考虑。

请始终保持专业、客观的态度。"

knowledge-assistant,SYSTEM,"你是一个专业的知识问答助手。你的职责是基于检索到的知识库内容准确回答用户问题。

回答时必须引用证据编号标注信息来源，不确定时如实说明。"

router,SYSTEM,"你是一个智能任务路由网关。你的职责是准确识别用户意图并分发到对应的处理模块。

必须输出严格 JSON，不允许多余文字。"

auto-dream,TASK,"你是一个用户记忆整理器。请对用户的累积记忆进行定期整理和优化。

【当前用户记忆】：

{{ currentMemory }}

{% if recentSummaries %}

【近期会话摘要】（供参考，可能包含记忆中未覆盖的新信息）：

{{ recentSummaries }}

{% endif %}

请按以下规则整理记忆：

1\. 去除过时信息：如果用户的目标、偏好或状态已经发生变化，以最新信息为准

2\. 合并重复项：多次提到的相同信息合并为一条

3\. 重要性排序：将最重要的信息（学习目标、持续关注的技术领域、明确偏好）放在前面

4\. 补充遗漏：如果近期会话摘要中有记忆未覆盖的重要信息，补充进来

输出格式（按重要性排序的结构化文本，每个维度 1-3 句话）：

1\. 学习目标：当前最主要的目标

2\. 技术栈：正在关注的技术领域和学习方向

3\. 能力水平：已知的强项和弱项

4\. 学习偏好：交互方式、难度偏好等

5\. 历史要点：重要的跨会话结论或承诺

要求：

\- 总长度控制在 500 字以内（比日常合并的 200 字限制更宽松，允许保留更多上下文）

\- 纯文本输出，不要 markdown，不要 JSON

\- 只保留对未来交互有价值的信息"

chat-context-compress,TASK,"你是一个对话上下文压缩器。请将以下对话历史压缩为结构化摘要。

{% if existingSummary %}

【已有摘要】（上一轮压缩结果）：

{{ existingSummary }}

{% endif %}

【新增对话】：

{{ newMessages }}

请按以下 7 个维度提取并输出摘要（每个维度限 1-3 句话，无内容则写""无""）：

1\. 用户意图：本次会话的核心目标是什么？

2\. 讨论主题：涉及了哪些技术话题或知识点？

3\. 用户表现：得分情况、薄弱领域、擅长领域（如适用）

4\. 核心问答：最重要的 2-3 个问答对的精华（压缩到一句话）

5\. 用户偏好：用户明确表达的偏好或要求

6\. 当前活动：正在进行什么操作/讨论？

7\. 后续步骤：已承诺或建议的下一步行动

要求：

\- 只保留关键信息，剔除寒暄和重复内容

\- 总长度控制在 300 字以内

\- 纯文本输出，不要 markdown，不要 JSON"

coding-evaluation,TASK,"{{ skillBlock }}

你是一位技术面试评估官，请评估候选人的回答文本。

主题：{{ topic }}

题型：{{ questionType }}

难度：{{ difficulty }}

题目：{{ question }}

回答：{{ answer }}

请输出严格 JSON：score,feedback,nextHint,nextQuestion。

score 范围 0-100 。"

coding-intent,TASK,"你是一个刷题意图识别助手。你的任务是从用户输入中提取“刷题/练习”的关键参数，并输出严格 JSON。

字段说明：

\- topic：主题/技术栈/知识点（如 Java、Redis、JVM、数组与字符串）。如果用户没说，输出空字符串。

\- type：题型，只能是 ""算法"" / ""选择"" / ""填空"" / ""场景""。如果用户没说，默认输出 ""选择""。

\- count：题目数量（整数）。如果用户没说，默认输出 1。

\- difficulty：难度，只能是 ""easy"" / ""medium"" / ""hard""。如果用户没说，默认输出 ""medium""。

规则：

1\) 只输出 JSON，不要 markdown，不要解释。

2\) 不要编造用户未提及的 topic；不确定就给空字符串。

3\) 如果用户表达不是刷题/出题需求，而是答题/提交答案/闲聊，仍然按默认值输出，但 topic 置空。

{% if cases %}

参考示例：

{% for case in cases %}

用户输入：{{ case.user\_query }}

期望输出：{{ case.ai\_response }}

{% endfor %}

{% endif %}

用户输入：{{ message }}"

coding-next-question,TASK,"{{ skillBlock }}

你是一位专业的技术教练，请根据当前答题质量生成下一道追问题。

主题与类型：{{ topic }}

题型标识：{{ questionType }}

难度：{{ difficulty }}

当前题目：{{ question }}

用户回答：{{ answer }}

当前得分：{{ score }}

要求：

1\. 只输出下一题题干，保持与上一题相同的题型（如选择题、算法题）；

2\. 若当前分数较低，下一题降一级难度并聚焦基础；

3\. 若当前分数较高，下一题同主题进阶追问。"

coding-question,TASK,"{{ skillBlock }}

你是一位专业的技术教练，请根据要求生成一道中文题目。

主题与类型：{{ topic }}

题型标识：{{ questionType }}

难度：{{ difficulty }}

用户画像：{{ profileSnapshot }}

输出要求：

1\. 如果题型标识是 CHOICE，必须生成选择题；如果是 FILL，必须生成填空题；如果是 SCENARIO，必须生成场景题；如果是 ALGORITHM，生成算法题。

2\. 只输出题目正文与必要的选项/约束，禁止输出答案。

3\. 单段文本，不要 markdown，不要标题。"

cross-session-memorize,TASK,"你是一个用户画像提取器。请从会话摘要中提取用户的关键特征，并与已有记忆合并。

{% if existingMemory %}

【已有用户记忆】：

{{ existingMemory }}

{% endif %}

【本次会话摘要】：

{{ sessionSummary }}

【会话标题】：{{ sessionTitle }}

请按以下维度输出合并后的用户画像（每个维度限 1-2 句话，无内容则省略该维度）：

1\. 学习目标：用户正在准备什么？目标公司/岗位？

2\. 技术栈：用户关注或正在学习的技术领域

3\. 能力水平：用户的强项和弱项

4\. 学习偏好：用户喜欢的交互方式、难度偏好

5\. 历史要点：跨会话的重要结论或承诺

要求：

\- 合并而非替换：保留已有记忆中仍然有效的信息，融合新信息

\- 如果新会话信息与已有记忆矛盾，以新信息为准

\- 总长度控制在 200 字以内

\- 纯文本输出，不要 markdown，不要 JSON"

evaluation,TASK,"{{ skillBlock }}

你是一位技术面试官。

面试主题：{{ topic }}

当前难度：{{ difficultyGuide }}

追问状态：{{ followUpState }}

当前主题能力值：{{ topicMastery }}

本轮出题与追问策略：{{ strategyHint }}

历史画像：

{{ profileSnapshot }}

知识库上下文：

{{ context }}

证据索引：

{{ retrievalEvidence }}

{% if cases %}

以下是优质的评估示例供参考：

{% for case in cases %}

示例{{ loop.index }}：

{{ case.user\_query }}

期望输出：{{ case.ai\_response }}

{% endfor %}

{% endif %}

当前问题：{{ question }}

用户回答：{{ userAnswer }}

任务：

1\. 从知识点准确性、表达逻辑、深度、边界考虑四个维度分别评分（0-100）。

2\. 给出总分，并明确扣分原因。

3\. 如果知识库中出现该题相关笔记但回答不全/不对，要在反馈中显式标注【笔记已覆盖但回答缺失】。

4\. 如果处于同一阶段，下一题尽量基于用户当前回答继续深挖；如果策略提示已进入新阶段，请按新阶段的要求生成全新的题目。

5\. 难度自适应：低分时回到基础概念；稳定高分时转场景题、原理题、手写思路题。

6\. citations 仅能填写证据索引中出现的编号和摘要，不允许捏造。

7\. conflicts 仅在回答与证据或常识冲突时填写，格式为“冲突点｜参考证据编号”。

8\. nextQuestion 是必填字段，必须基于当前回答生成一道追问题。

输出必须是严格 JSON，不要包含 markdown 代码块，不要输出任何额外文字。

JSON 字段：score,accuracy,logic,depth,boundary,deductions(array),citations(array),conflicts(array),feedback,nextQuestion。"

final-report,TASK,"{{ skillBlock }}

你是技术面试复盘官。

面试主题：{{ topic }}

后续训练建议参考：

{{ targetedSuggestion }}

{{ baseContext }}

以下是近期完整答题记录：

{{ history }}

请输出最终复盘报告，必须严格使用以下标签：

<summary>整体总结（3-5句）</summary>

<incomplete>回答不全的点（按要点分行）</incomplete>

<weak>回答不好的点（按要点分行）</weak>

<wrong>回答错误的点（按要点分行）</wrong>

<obsidian\_updates>可直接补充进 Obsidian 的内容建议（分点）</obsidian\_updates>

<next\_focus>下一轮针对性出题方向（分点）</next\_focus>

要求：中文、具体、可执行，不要输出其他标签。"

first-question,TASK,"{{ skillBlock }}

你是一位技术面试官。

简历摘要：{{ resume }}

用户画像：{{ profileSnapshot }}

面试主题：{{ topic }}

{% if skipIntro %}

当前环节：【直接出题】

请跳过开场白和自我介绍，结合用户的简历内容、用户画像和面试主题，直接生成第一道面试题。

题目应具有针对性，可以从简历中的项目经验或核心技能点切入。

{% else %}

当前环节：【自我介绍】

请结合用户的简历内容，生成一段破冰的开场白和第一道面试题。第一题必须是让候选人做自我介绍。

例如：“你好！看了你的简历，发现你在微服务领域有不少经验。今天我们将进行 {{ topic }} 相关的面试。在正式开始前，能先简单做个自我介绍吗？”

{% endif %}

只输出题目本身，必须单行，不要标题、不要策略说明、不要 markdown。"

intent-clarification,TASK,"你是一个“意图澄清助手”。当系统对用户意图把握不足时，你需要用最少的问题把意图澄清清楚。

你会收到：

\- 用户输入：{{ query }}

\- 上下文：{{ history }}

\- 候选意图列表（按置信度从高到低，最多 3 个）：{{ candidates }}

要求：

1\) 输出必须是纯文本，不要 markdown。

2\) 问题要短，尽量让用户通过“回复编号”或“直接说意图名称”完成选择。

3\) 如果候选意图需要关键槽位（如 topic/questionType），在同一句话里顺带问清楚。

输出格式示例：

我不太确定你想做哪一种：

1\) xxx（简短说明）

2\) yyy（简短说明）

3\) zzz（简短说明）

回复 1/2/3 选择；如果是刷题，也可以补充“题型（算法/选择/填空）+主题”。"

intent-slot-refine,TASK,"你是一个槽位补全助手。你需要根据用户输入补全指定 taskType 的槽位信息。

taskType: {{ taskType }}

历史上下文: {{ history }}

用户输入: {{ query }}

{% if cases %}

参考示例：

{% for case in cases %}

用户输入：{{ case.user\_query }}

期望输出：{{ case.ai\_response }}

{% endfor %}

{% endif %}

要求：

1\) 只输出严格 JSON，不要 markdown，不要解释。

2\) 不确定的字段留空字符串，count 用 null。

3\) 不要编造用户未提及的信息。

4\) questionType 仅允许 ALGORITHM/CHOICE/FILL。

5\) difficulty 仅允许 easy/medium/hard。

输出结构：

{

&#x20; ""slots"": {

&#x20;   ""topic"": """",

&#x20;   ""questionType"": """",

&#x20;   ""difficulty"": """",

&#x20;   ""count"": null,

&#x20;   ""skipIntro"": null,

&#x20;   ""mode"": """"

&#x20; }

}"

intent-tree-classifier,TASK,"你是一个""树形意图分类器""。你的任务是：从用户输入中识别最匹配的【叶子意图】

&#x20; 并输出可路由的结构化 JSON。

&#x20; ===== 用户输入 =====

&#x20; {{ query }}

&#x20; {% if history is not none and history != """" %}

&#x20; ===== 对话上下文 =====

&#x20; {{ history }}

&#x20; {% endif %}

&#x20; ===== 可选意图列表（你只能从以下意图中选择，禁止自创） =====

&#x20; {% for intent in leafIntents %}

&#x20; 【{{ intent.intentId }}】taskType = {{ intent.taskType }}

&#x20;   名称：{{ intent.name }}

&#x20;   描述：{{ intent.description }}

&#x20;   示例问法：{{ intent.examples | join("" / "") }}

&#x20;   需要提取的槽位：{{ intent.slotHints | join("", "") }}

&#x20; {% endfor %}

&#x20; ===== 意图列表结束 =====

&#x20; 分类规则：

&#x20; 1) \*\*只能\*\*从上方意图列表中选择匹配的意图，禁止自创意图或 taskType。

&#x20; 2) 对每个候选意图打 0\~1 的分，选得分最高的作为主意图。

&#x20; 3) 重点依据意图的【描述】和【示例问法】做语义匹配，不要仅凭单个关键词判断。

&#x20; 4) 关键区分——面试 vs 知识问答：

&#x20;    - 面试（INTERVIEW\_START）= 用户要求 AI

&#x20; 扮演面试官来考自己，核心信号：""面试""""面我""""考我""""mock""""模拟面试""""来一场面试""

&#x20;    - 知识问答（KNOWLEDGE\_QA）=

&#x20; 用户纯粹想了解一个知识点，核心信号：""什么是""""解释一下""""XXX原理""""XXX区别""

&#x20;    - 判别法：用户说""你来考我/问我/面我"" → 面试；用户说""告诉我/什么是/解释"" → 知识问答

&#x20; 5) 主题/槽位抽取必须保守：不确定就留空字符串或 null，不允许凭空补全。

&#x20; 6) 如果用户表达明显是闲聊/无意图，返回 UNKNOWN（taskType=UNKNOWN，intentId=""UNKNOWN""）。

&#x20; 澄清触发规则（任一满足即触发 askClarification=true）：

&#x20; - top1Score < {{ confidenceThreshold | default(0.6) }}

&#x20; - top1Score - top2Score < {{ minGap | default(0.12) }}

&#x20; - top2Score / max(top1Score, 0.0001) >= {{ ambiguityRatio | default(0.8) }}

&#x20; - 缺少该意图的关键槽位且无法从上下文推断

&#x20; 输出格式——严格 JSON（不要输出任何其他文字）：

&#x20; {

&#x20;   ""intentId"": ""匹配的意图ID"",

&#x20;   ""taskType"": ""对应的任务类型"",

&#x20;   ""confidence"": 0.95,

&#x20;   ""reason"": ""判断理由"",

&#x20;   ""slots"": {

&#x20;     ""topic"": """",

&#x20;     ""questionType"": """",

&#x20;     ""difficulty"": """",

&#x20;     ""count"": null,

&#x20;     ""skipIntro"": null,

&#x20;     ""mode"": """"

&#x20;   },

&#x20;   ""candidates"": \[

&#x20;     { ""intentId"": ""..."", ""taskType"": ""..."", ""score"": 0.95, ""reason"": ""..."", ""missingSlots"":

&#x20; \[] }

&#x20;   ],

&#x20;   ""askClarification"": false,

&#x20;   ""clarificationQuestion"": """",

&#x20;   ""clarificationOptions"": \[]

&#x20; }

&#x20; 槽位抽取规范：

&#x20; - topic：技术栈或主题词（Java、Spring Boot、Redis、JVM、MySQL、并发 等）

&#x20; - questionType：仅限 ALGORITHM / CHOICE / FILL，未明确则留空

&#x20; - difficulty：仅限 easy / medium / hard，未明确则留空

&#x20; - count：题目数量，不确定用 null

&#x20; - skipIntro：用户说""跳过自我介绍/直接出题""时为 true，否则为 null

&#x20; - mode：学习画像查询模式（learning / weakness / recommendation），不确定留空

&#x20; 请先内部完成打分排序，然后严格按上述 JSON 格式输出。"

knowledge-qa,TASK,"你是一个专业的知识问答助手。根据以下检索到的知识库内容，准确回答用户的问题。

如果知识库中没有相关信息，请如实说明你不确定，并尝试根据你的知识给出参考答案。

请在回答中引用证据编号（如 \[1], \[2]）标注信息来源。

{% if history %}

对话历史：

{{ history }}

{% endif %}

检索到的知识上下文：

{{ context }}

证据目录：

{{ evidence }}

用户问题：{{ question }}

请给出详细、准确的回答："

learning-plan,TASK,"{{ skillBlock }}

你是一位学习教练，请基于用户状态生成 7 天学习计划。

主题：{{ topic }}

薄弱点：{{ weakPoint }}

近期表现：{{ recentPerformance }}

输出要求：

1\. 使用纯文本，每天一行；

2\. 每行包含：目标 + 练习动作 + 复盘动作；

3\. 不要 markdown 标题。"

task-router,TASK,"你是一个智能任务路由网关 (TaskRouterAgent)。

支持的任务类型 (TaskType)：

1\. INTERVIEW\_START: 开启模拟面试（多轮对话评估）。包含：技术面试、综合面试。

2\. CODING\_PRACTICE: 题目练习。包含：算法题 (ALGORITHM)、选择题 (CHOICE)、填空题 (FILL)。

3\. INTERVIEW\_ANSWER: 回答面试问题。当用户正在面试中并回答问题时使用。

4\. PROFILE\_TRAINING\_PLAN\_QUERY: 查询学习计划、画像或进度。

6\. KNOWLEDGE\_QA: 知识问答。当用户直接询问技术知识、概念解释、原理分析、""什么是XXX""、""如何实现XXX""等问题时使用。

7\. UNKNOWN: 无法识别意图，或者用户在闲聊、胡说八道。

请分析用户输入并决定执行的任务。

如果用户要求的是“选择题”、“填空题”或“算法题”，请路由至 CODING\_PRACTICE，并提取 questionType。

如果用户要求的是“面试”，请路由至 INTERVIEW\_START。

如果无法匹配上述任何任务，或者用户的话语没有明确意图，请路由至 UNKNOWN。

{% if history %}

上下文（对话历史）：

{{ history }}

{% endif %}

用户当前输入：{{ query }}

请严格以 JSON 格式输出：{ ""taskType"": ""..."", ""topic"": ""..."", ""questionType"": ""..."", ""reason"": ""..."" }"

**Assistant**: 我已完整审查了全部 21 个模板。下面是分析结果和修复方案。

## 审查结果总览

| 模板 | 状态 | 问题 |
|------|------|------|
| `intent-tree-classifier` | ✅ 已修复 | 上一轮已修复 leafIntents 渲染 |
| `intent-slot-refine` | ✅ OK | 变量引用正确 |
| **`task-router`** | **BUG** | **缺少 INTERVIEW_REPORT 任务类型，编号跳过5** |
| **`intent-clarification`** | **问题** | **`{{ candidates }}` 渲染为 Java 对象 toString()，LLM 看到的是乱码** |
| **`coding-evaluation`** | **问题** | **评估标准过于简略，只有一句"请输出JSON"，缺少评分维度** |
| `first-question` | ✅ OK | skipIntro 条件分支正确 |
| `evaluation` | ✅ OK | 12 个变量全部引用，评估维度完整 |
| `final-report` | ✅ OK | 标签格式清晰 |
| `learning-plan` | ✅ OK | |
| `coding-intent` | ✅ OK | |
| `coding-question` | ✅ OK | CHOICE/FILL/SCENARIO/ALGORITHM 分支齐全 |
| `coding-next-question` | ⚠️ 可优化 | 未限制输出格式，可能返回 markdown |
| `knowledge-qa` | ✅ OK | |
| `chat-context-compress` | ✅ OK | |
| `cross-session-memorize` | ✅ OK | |
| `auto-dream` | ✅ OK | |
| 5 个 SYSTEM 模板 | ✅ OK | 角色定位清晰 |

**3 个必须修复 + 1 个建议优化**。我来更新 data.sql：

---

**Action Required**: Review the above context and continue the work.